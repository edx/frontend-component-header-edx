#check package-lock file version

name: Lockfile Version check

on:
  push:
    branches:
    - master
  pull_request:

jobs:
  version-check:
    uses: uses: openedx/.github/.github/workflows/lockfileversion-check-v3.yml@master
